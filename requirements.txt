# PyTorch is installed by the Dockerfile from the TORCH_INDEX_URL build arg so
# the build (CPU vs CUDA) can be chosen; these pins keep dev installs working.
torch>=2.5
torchvision>=0.20
snntorch>=1.0
matplotlib>=3.8
Pillow>=10.0
numpy>=1.26
fastapi>=0.110
uvicorn[standard]>=0.27
websockets>=12.0
pydantic>=2.5
psutil>=5.9

# NIR interop: the official Neuromorphic Intermediate Representation packages.
# They are isolated behind snn_interpreter/nir_bridge/api.py and are installed
# by default so the Docker image can probe them (see the `nir` extra too).
nir>=1.0
nirtorch>=1.0
