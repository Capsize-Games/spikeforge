# Core runtime pins usable for a plain `pip install -r requirements.txt` dev
# setup. PyTorch is installed by the Dockerfile from the TORCH_INDEX_URL build
# arg so the build (CPU vs CUDA) can be chosen; these pins keep dev installs
# working.
#
# The FastAPI server stack (fastapi/uvicorn/websockets/pydantic) is intentionally
# absent: it is now the base dependency set of the `spikeforge-server`
# distribution, so core stays headless.
torch>=2.5
torchvision>=0.20
snntorch>=1.0
matplotlib>=3.8
Pillow>=10.0
numpy>=1.26
psutil>=5.9

# NIR interop: the official Neuromorphic Intermediate Representation packages.
# They are isolated behind spikeforge/nir_bridge/api.py and are installed
# by default so the Docker image can probe them (see the `nir` extra too).
nir>=1.0
nirtorch>=1.0
